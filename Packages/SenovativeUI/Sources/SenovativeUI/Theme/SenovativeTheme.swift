import SwiftUI

public enum SenovativeTheme {
    public static let accent = Color(red: 0.00, green: 0.36, blue: 0.74)
    public static let canvasBackground = Color(nsColor: .textBackgroundColor)
    public static let chromeBackground = Color(nsColor: .windowBackgroundColor)
    public static let divider = Color(nsColor: .separatorColor)
}
